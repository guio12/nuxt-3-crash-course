/** @type {import('tailwindcss').Config} */
module.exports = { content: ['./src/**/*.{html,js}'], theme: {} }

/* had to create this file because the tailwind autocomplete wouldn't work without it for some reason */
